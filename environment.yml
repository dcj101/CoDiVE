name: codive
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip>=23
  - setuptools>=68
  - wheel
  - pytorch
  - torchvision
  - pytorch-cuda=12.1
  - transformers
  - datasets
  - accelerate
  - pandas
  - scikit-learn
  - tqdm
  - pyyaml
  - pillow
  - numpy
  - opencv
  - huggingface_hub
  - pip:
      - decord
      - qwen-vl-utils
      # Optional, install manually if needed and supported by your GPU environment:
      # - unsloth
      # - vllm
